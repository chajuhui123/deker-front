import React from "react";
// import { useAlert } from "react-alert";

function CommPopup() {
  //   const alert = useAlert();

  return (
    <div>
      <button
        onClick={() => {
          //   alert.show("This is Alert Test");
        }}
      >
        테스트 버튼
      </button>
    </div>
  );
}

export default CommPopup;
